import { Box, Typography } from "@mui/material";

const Home = () => {
  return(
     <Box>
        <Typography variant="h3">
          Welcome To home page
        </Typography>
     </Box>
  );
};

export default Home;